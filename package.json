{
  "name": "sec-game-ar",
  "version": "1.0.0",
  "description": "AR Salted Egg Challenge - Educational Game",
  "scripts": {
    "git:push": "git add . && git commit -m \"Update game\" && git push",
    "gas:push": "clasp push",
    "push:all": "npm run git:push && npm run gas:push"
  }
}
